let ! repetici�n nula = no debe tener efectos visibles

  var k init 11 

in

  loop for i from k to 10 do ! determinar que k se eval�a una sola vez, al iniciar el ciclo
  ! ciclo debe ser vac�o (no ejecutar el cuerpo)

    k := k + 1 ; 

    putint (1) ! ejecuci�n nula, no se imprime

  end

end